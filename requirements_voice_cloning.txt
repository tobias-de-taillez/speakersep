# 🎤 Voice Cloning Requirements v2.0 - Python 3.12 Compatible
# State-of-the-Art Models: F5-TTS (Primary) + XTTS-v2 (Fallback) + Zonos-v0.1 (Experimental)

# Core PyTorch with MPS support for M4 Pro
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# F5-TTS - Primary Model (Most Realistic)
f5-tts>=1.1.0  # Official F5-TTS library
f5-tts-mlx>=0.1.0  # MLX optimized for Apple Silicon (optional)

# Transformers and HuggingFace
transformers>=4.40.0
datasets>=2.15.0
accelerate>=0.25.0
huggingface_hub>=0.20.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.0
numpy>=1.24.0
resampy>=0.4.0

# TTS Models
TTS>=0.22.0  # Coqui TTS for XTTS-v2
phonemizer>=3.2.0  # For F5-TTS
cached-path>=1.6.0

# Configuration and utilities
omegaconf>=2.3.0
hydra-core>=1.3.0
pydantic>=2.0.0

# Audio utilities
pydub>=0.25.0
pedalboard>=0.7.0

# Web interface
gradio>=4.0.0
fastapi>=0.104.0
uvicorn>=0.24.0

# Monitoring and logging
tensorboard>=2.15.0
wandb>=0.16.0

# Development tools
jupyter>=1.0.0
ipython>=8.12.0

# Additional dependencies for model compatibility
sentencepiece>=0.1.99  # For OpenVoice compatibility
tokenizers>=0.15.0
safetensors>=0.4.0 